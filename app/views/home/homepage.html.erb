<script>
	$twitter_client.widgets.createTimeline(
  {
    sourceType: 'profile',
    screenName: 'twitterdev'
  },
  document.getElementById('timeline'),
  {
    width: '450',
    height: '700',
    related: 'twitterdev,twitterapi'
  }).then(function (el) {
    console.log('Embedded a timeline.')
  });
</script>     

<% $twitter_client.search("to:justinbieber marry me", result_type: "recent").take(1).each do |tweet| %>
  <%= tweet.text %>
  <%#= p tweet.inspect %>
  <%#= p tweet.to_yaml%>
  <br>
<% end %>

